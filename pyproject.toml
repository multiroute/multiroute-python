[project]
name = "multiroute"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Javohir Ismatullayev", email = "70445308+ismatullayevs@users.noreply.github.com" }
]
requires-python = ">=3.13"
dependencies = [
    "anthropic>=0.84.0",
    "google-genai>=1.66.0",
    "openai>=2.26.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "pytest-asyncio>=1.3.0",
]
