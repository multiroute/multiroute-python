# URL registry: maps base URL substrings to provider names.
# When a request is made without a provider prefix (e.g. "gpt-4o" instead of
# "openai/gpt-4o"), the resolver inspects the client's base_url and looks up
# this file to prepend the correct provider so the Multiroute proxy knows how
# to route the request.
#
# Format:
#   url_providers:
#     <url-substring>: <provider>
#
# Matching checks whether the base_url contains the substring (case-insensitive).
# Entries are tested longest-first so more specific URLs win.

url_providers:
  api.openai.com: openai
  api.groq.com: groq
  api.anthropic.com: anthropic
  generativelanguage.googleapis.com: google
  api.mistral.ai: mistral
  api.cohere.com: cohere
  api.cohere.ai: cohere
  api.deepseek.com: deepseek
  api.x.ai: xai
  api.together.xyz: together
  api.fireworks.ai: fireworks
  api.perplexity.ai: perplexity
  openrouter.ai: openrouter
  api.cerebras.ai: cerebras
  inference.ai.azure.com: azure
  api.cognitive.microsoft.com: azure
  openai.azure.com: azure
